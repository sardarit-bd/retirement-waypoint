import { AdminContactCms } from '@/features/contact-cms/components/AdminContactCms';

export const metadata = {
  title: 'Contact Page CMS | Admin Dashboard',
  description: 'Manage Contact Page headlines, email address, phone number, and message form settings.',
};

export default function AdminContactCmsPage() {
  return <AdminContactCms />;
}
